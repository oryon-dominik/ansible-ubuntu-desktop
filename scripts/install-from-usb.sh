# TODO: basic installation steps for a new machine and a fresh install

# wget --output-document=- https://raw.githubusercontent.com/oryon-dominik/ansible-ubuntu-desktop/trunk/setup-ansible-provisioning.sh | bash

# Run the playbooks locally to start provisioning.

# TODO: one playbook to control them all
# ansible-playbook copy-ssh-to-usb.yml --ask-become-pass"
# ansible-playbook install-system.yml --ask-become-pass"
# ansible-playbook install-essential-software.yml --ask-become-pass"
# ansible-playbook install-system-configuration.yml --ask-become-pass"
